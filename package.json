{
  "name": "altura-nft-passs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
  "web:dev": "npm --prefix web run dev",
  "web:build": "npm --prefix web run build",
  "web:preview": "npm --prefix web run preview",
  "deploy:local": "hardhat run scripts/deploy-altura.ts --network hardhat",
  "deploy:sepolia": "hardhat run scripts/deploy-altura.ts --network sepolia",
  "meta:gen": "ts-node scripts/generate-metadata.ts",
  "baseuri:set": "hardhat run scripts/set-baseuri.ts --network sepolia",
  "verify:sepolia": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.30",
    "chai": "^4.4.1",
    "hardhat": "^2.22.10",
    "ts-node": "^10.9.2",
  "typescript": "^5.4.5"
  },
  "dependencies": {
  "@openzeppelin/contracts": "^5.0.2",
  "dotenv": "^16.4.5"
  }
}
